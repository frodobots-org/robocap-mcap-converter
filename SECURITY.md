# Security

Do not report security vulnerabilities in public issues. Use GitHub's private
vulnerability reporting feature, or email `contact@bitrobot.ai` with
reproduction steps and affected versions.

The converter does not require FrodoBots credentials or call a FrodoBots API.
For S3 conversion, use short-lived workload credentials scoped to the input
and output prefixes.
